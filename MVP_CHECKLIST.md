# MVP Checklist

Сверка с исходным запросом.

## Сообщения

- Текст - есть.
- Голосовые - есть, через `faster_whisper`.
- Фото с подписью - есть.
- AI-описание изображения - есть через `GROQ_VISION_MODEL`, если включён `PHOTO_VISION_ENABLED`.
- Фото без подписи - есть, сохраняется в альбом и создаётся карточка `Фото без подписи` без выдуманного анализа изображения, если vision/OCR ничего не дали.
- OCR текста на фото - есть через Tesseract, если он установлен.
- Альбом загруженных фото - есть.
- Удаление фото из альбома и локального хранилища - есть.
- Ссылки - есть, бот пытается подтянуть `<title>`.
- Пересланные сообщения - есть, обрабатываются как обычный текст/медиа от пользователя.
- Админка - есть, кнопка `🛡 Админка` видна только администраторам, команда `/admin` тоже работает.

## AI-разбор

- Коротко - есть.
- Разделение длинных мыслей на карточки - есть.
- Выделение задач - есть.
- Следующий шаг - есть.
- Автоопределение типа - есть.
- Автоопределение категории - есть.

## Экраны

- Первый экран - есть.
- `Как это работает` - есть.
- Главное меню - есть.
- Мысли - есть, одним списком с пагинацией без повторной рассылки карточек.
- Продолжить мысль - есть, с выбором карточки, кнопкой `Продолжить`, подробностями и архивированием.
- Поиск - есть.
- Архив - есть.
- Альбом - есть.
- Подробности карточки - есть, с действиями `Продолжить`, `Переименовать`, `Категория`, `Архивировать`, `Удалить`.

## UX

- Ручная сортировка не нужна.
- Категории второстепенны и не вынесены в основное меню.
- Карточки короткие.
- На короткой карточке только `Подробнее` и `Архивировать`; редактирование лежит в подробностях.
- Длинные AI-анализы убраны из основной карточки.

## Проверка

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tests
.\.venv\Scripts\python.exe -m compileall -q src tests
```

Обе проверки проходят.

`startup_check` на текущем локальном `.venv` ожидаемо останавливается, потому что там Python 3.14. Для реального Whisper на Windows нужен Python 3.12 или Docker.
